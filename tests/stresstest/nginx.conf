
    # resolver 127.0.0.11 ipv6=off;
    # location / {
    #     try_files $uri @wsgi;
    # }

    # location @wsgi {
    #     set $target test-checker;
    #     proxy_pass http://$target:3031;
    # }

server {
        listen 80;
        location / {
        proxy_pass http://test-checker:3031;
        }
}


    #     set $target test-checker;
    #     proxy_pass http://$target:3031;