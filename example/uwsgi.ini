[uwsgi] 
enable-threads = true
module = testchecker
callable = app
uid = www-data
gid = www-data

threads = 10
cheaper-algo = spare
cheaper = 100
cheaper-initial = 100
# maximum number of workers that can be spawned
workers = 1000
# how many workers should be spawned at a time
cheaper-step = 20
